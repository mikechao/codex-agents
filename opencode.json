{
  "$schema": "https://opencode.ai/config.json",
  "default_agent": "orchestrator",
  "mcp": {
    "workflow_state": {
      "type": "local",
      "command": ["bun", "--no-warnings", ".codex/workflow-mcp/bootstrap.ts"],
      "enabled": true,
      "timeout": 30000
    }
  }
}
